[defaults]
roles_path = ./roles/
host_key_checking = false
inventory = ./inventory
interpreter_python = auto_silent
hash_behaviour = merge
collections_paths: ../collections:~/.ansible/collections:/usr/share/ansible/collections:./collections

[persistent_connection]
connect_timeout = 60
command_timeout = 60
